<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.jninber.client">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".main.SplashActivity"
            android:theme="@style/StartStyle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main.MainActivity" />
        <activity
            android:name=".edit.EditCategoryActivity"
            android:label="@string/title_activity_edit_menu" />
        <activity android:name=".edit.EditMenuDetailActivity" />
        <activity
            android:name=".menu.DetailActivity"
            android:label="菜单详情" />
        <activity android:name=".menu.CategoryActivity" />
    </application>

</manifest>